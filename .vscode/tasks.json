{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with GCC 13",
            "type": "shell",
            "command": "/opt/homebrew/bin/g++-13",
            "args": [
                "-std=c++17",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

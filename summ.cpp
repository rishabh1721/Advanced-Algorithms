#include <iostream>
using namespace std;
int main()
{
  // int n, sum;
  // cin >> n;
  // sum = 0;
  // for (int i = 1; i <= n; i++)
  // {
  //   sum = sum + i;
  // }
  // cout << sum;
  // return 0;
  int n;
  cin >> n;
  int sums = n * (n + 1) / 2;
  cout << sums;
}
